@model IEnumerable<Screen>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ScreenID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScreenName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScreenDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ScreenID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScreenName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScreenDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ScreenID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ScreenID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ScreenID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
